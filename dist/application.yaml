server:
  port: 1080
spring:
  thymeleaf:
    prefix: file:/home/mrr4/mealtracker/app/frontend/templates/
    cache: false # pozwala na żywo przełądowywać themy

  datasource:
    url: "jdbc:mysql://localhost:3306/meal_tracker_dev"
    username: meal
    password: meal
    pool-size: 30
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      properties:
      hibernate:
        format_sql: true
        type: trace
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
